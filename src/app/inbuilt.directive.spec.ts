import { InbuiltDirective } from './inbuilt.directive';

describe('InbuiltDirective', () => {
  it('should create an instance', () => {
    const directive = new InbuiltDirective();
    expect(directive).toBeTruthy();
  });
});
